{
  "name": "@comparex/database",
  "version": "1.0.0",
  "description": "Database migrations and schemas",
  "scripts": {
    "migrate:up": "db-migrate up",
    "migrate:down": "db-migrate down",
    "migrate:create": "db-migrate create"
  },
  "dependencies": {
    "db-migrate": "^0.11.14",
    "db-migrate-pg": "^1.5.2"
  }
}
